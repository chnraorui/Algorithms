#include<iostream>
using namespace std;

int randSelect(){
    if(left == right)return A[left];
    int p = randPartirion(A,left,right);
    int M = p-left+1;
    if(k==M) return A[p]
}
int main()
{
    return 0;
}